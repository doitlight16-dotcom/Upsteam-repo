# Базовый URL backend API. Пока backend-эндпоинты для этого frontend не
# реализованы, приложение использует mock-данные (см. src/api/*.ts) и эта
# переменная не обязательна.
#
# Когда endpoints появятся — задайте значение и переключите активную
# реализацию в src/api/{schedule,participants,concierge,whiteLabel}Api.ts
# с Mock*Api на Http*Api.
VITE_API_BASE_URL=https://api.appex-asset-suite.example.com/api/v1
