"""Adapters implementing the AIModulePort against whatever backend is chosen.

An HTTP adapter calling the ai-module service is added in a later step;
the port itself is defined in application/interfaces.
"""
