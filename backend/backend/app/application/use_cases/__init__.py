"""Use cases: one class or function per application action.

Use cases depend only on interfaces from application/interfaces and
objects from domain/, never on a concrete database session or HTTP
client directly.
"""
